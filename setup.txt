Setting up new laptops
1) init a catkin workspace
2 copy over all of the files froom the pharos stack
	outdoor navigatin
	pan_tilt_node
	all of the proteus notes
	teleop
	ackaman drive\
if things aren't working leave out that package then build the worspace adn that should fix the problem

SETUP
connect to the internet
run
$ chmod 755 setup
$ source ./setup

then to set the port setup
get on as super user
$ sudo su
plug in the motor thing
$ source motorPort
unplug then plug in the gps
$ source gpsPort
now set the udev rules
############################################################################
this is how explaing stuff
##########################################################################

to run the scripts to test things

1) $ source indigo

2)
	a) to test the motor run
	$ roslaunch traxxas_node test_drive.launch
	b) to test the motor run
	$ roslaunch proteus3_compass_hydro test_compass.launch
	c) to test the gps run
	$ roslaunch proteus3_gps_hydro test_gps.launch
